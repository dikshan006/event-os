/**
 * Stub for the `server-only` package under Vitest.
 *
 * The real module exists to make a client-side import fail at build time. It
 * has no runtime behaviour to reproduce, and the test runner is not a client
 * bundle, so an empty module is the whole of it.
 */
export {};
